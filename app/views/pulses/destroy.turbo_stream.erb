<%# turbo_stream.remove("#{@pulse.hashid}_item") %>

<%= turbo_stream.replace(:header_pulse_list) do %>
 <%= render partial: "pulses/header_list", locals: { quantity_of_pulses: @pool.pulses.size, total_pulses: @pool.quantity } %>
<% end %>

<% if @pool.pulses.none? %>
  <%= turbo_stream.replace(:pulse_details) do %>
    <%= render partial: "pools/empty_state", locals: { pool: @pool } %>
  <% end %>
<% end %>
